# 健身 App 首页

## 1. 简要概述
健身 App 首页是一个完整的移动端应用首页实现，展示了 Axhub 平台的标准开发模式。组件采用 React + TypeScript 开发，遵循 Axure API 规范，支持完整的事件、动作、变量和配置管理。主要功能包括用户运动数据统计、每日目标进度展示、课程推荐和底部导航。

## 2. 功能说明
按照页面或元素展开说明所有功能，需要将每个模块都详细展开，不要遗漏任何部分。

### 2.1 页面头部
- **组成结构**：用户头像、用户名、通知图标
- **内容类型**：
  - 用户头像：圆形头像图片
  - 用户名：可配置的用户名文本
  - 通知图标：铃铛图标
- **布局和样式**：
  - 水平排列，头像在左，通知图标在右
  - 使用固定高度
  - 背景色为深色主题
- **交互行为**：无交互（可扩展）
- **状态变化**：无状态变化

### 2.2 运动数据统计区域
- **组成结构**：三个统计卡片（卡路里消耗、运动时长、连续天数）
- **内容类型**：
  - 图标：表情符号图标（🔥、⏱️、🔥）
  - 数值：统计数字
  - 标签：说明文字
- **布局和样式**：
  - 三个卡片水平排列
  - 支持横向滚动
  - 卡片使用半透明背景和阴影
  - 圆角设计
- **交互行为**：
  - 点击卡片有缩放反馈效果
  - 可扩展为触发事件
- **状态变化**：无状态变化（数据为静态展示）

### 2.3 每日目标区域
- **组成结构**：
  - 标题："今日计划"
  - 环形进度条
  - 进度百分比文字
  - 目标说明文字
  - "开始训练"按钮
- **内容类型**：
  - 进度值：0-100 的数字
  - 目标值：可配置的每日目标卡路里
- **布局和样式**：
  - 垂直居中布局
  - 使用 SVG 绘制环形进度条
  - 进度条颜色使用强调色
  - 按钮使用强调色背景
- **交互行为**：
  - 点击"开始训练"按钮：触发 `onStartWorkout` 事件
  - 进度条根据进度值动态显示
  - 可通过 `updateProgress` 动作更新进度
- **状态变化**：
  - `todayProgress` 变量：当前进度值（0-100）

### 2.4 课程推荐区域
- **组成结构**：
  - 区域标题："推荐课程"
  - 横向滚动的课程卡片列表
- **内容类型**：
  - 课程卡片包含：
    - 封面图片
    - 课程标题
    - 课程时长
    - 难度等级
    - 分类标签
- **布局和样式**：
  - 横向滚动布局，隐藏滚动条
  - 卡片使用图片背景 + 渐变遮罩
  - 分类标签使用强调色背景
  - 卡片固定最小宽度（240px）
- **交互行为**：
  - 点击课程卡片：触发 `onCourseClick` 事件，传递课程对象
  - 鼠标悬停时图片放大效果
- **状态变化**：无状态变化

### 2.5 底部导航栏
- **组成结构**：四个导航项（首页、计划、统计、我的）
- **内容类型**：
  - 导航图标：SVG 图标
  - 导航标签：文字说明
- **布局和样式**：
  - 固定在页面底部
  - 四个导航项均匀分布
  - 选中项使用强调色高亮
  - 适配 iPhone 底部安全区
- **交互行为**：
  - 点击导航项：切换当前选中的 Tab，触发 `onTabChange` 事件，传递 Tab 索引
  - 选中项有视觉高亮
- **状态变化**：
  - `currentTab` 变量：当前选中的 Tab 索引（0-3）

## 3. Axure API 说明
> 如果没有使用 Axure API，则不需要编写此部分。

### 事件列表
| 事件名 | 描述 | 触发时机 | 参数 |
|--------|------|----------|------|
| `onCourseClick` | 点击课程卡片时触发 | 用户点击任意课程卡片 | `{ course: Course }` - 课程对象 |
| `onStartWorkout` | 点击开始训练时触发 | 用户点击今日计划中的开始按钮 | `{}` |
| `onTabChange` | 切换底部标签栏时触发 | 用户点击底部导航任意 Tab | `{ index: number }` - Tab 索引 |

### 动作列表
| 动作名 | 描述 | 参数 |
|--------|------|------|
| `refreshData` | 刷新首页数据 | 无 |
| `updateProgress` | 更新今日目标进度 | `{ progress: number }` - 进度值（0-100） |

### 变量列表
| 变量名 | 类型 | 描述 |
|--------|------|------|
| `currentTab` | `number` | 当前选中的标签页索引（0-3） |
| `todayProgress` | `number` | 今日目标完成进度（0-100） |

### 配置项列表
| 配置项ID | 类型 | 显示名称 | 说明 | 默认值 |
|----------|------|----------|------|--------|
| `userName` | `input` | 用户名 | 显示在页面顶部的用户名 | `'Alex'` |
| `accentColor` | `colorPicker` | 强调色 | App 的主要强调色，影响按钮、进度条、选中状态等 | `'#a6ff00'` |
| `dailyGoal` | `inputNumber` | 每日目标(kcal) | 每日卡路里消耗目标值 | `500` |

### 数据项列表
| 数据项名称 | 描述 | 数据结构 |
|-----------|------|----------|
| `courses` | 推荐课程列表 | `Array<Course>` |

**Course 字段说明：**
- `id` (number)：课程唯一标识
- `title` (string)：课程标题
- `duration` (number)：课程时长（分钟）
- `level` (string)：难度等级
- `category` (string)：分类标签
- `image` (string)：封面图片 URL

**数据示例：**
```json
{
  "courses": [
    {
      "id": 1,
      "title": "HIIT 高强度燃脂",
      "duration": 20,
      "level": "K3",
      "category": "减脂",
      "image": "https://images.unsplash.com/photo-1517836357463-d25dfeac3438"
    }
  ]
}
```
