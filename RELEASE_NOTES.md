# Axhub Make 0.6.20-beta.1

- 从当前完整的 Make 产品主线重新构建测试版，修正 `0.6.20-beta.0` 使用了错误发行基线的问题
- 移除空白页中的旧版“我们先从哪里开始呢?”引导面板，同时保留原型案例与主题模板库
- 在项目设置中提供 5 个可读、可编辑并可按需恢复的固定模板
- 绑定 Client Template 0.1.20，包含本地主题检索技能、Design Knowledge 和 223 份经哈希校验的设计知识
- 保持 `@axhub/acp` 为 0.1.13，并继续使用 npm `beta` 标签发布，不影响 `latest`
